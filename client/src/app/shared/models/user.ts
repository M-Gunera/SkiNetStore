export interface User {
    email: string;
    displayName: string;
    token: string;
}

export interface Root {
    firstName: string;
    lastName: string;
    street: string;
    city: string;
    zipCode: string;
}